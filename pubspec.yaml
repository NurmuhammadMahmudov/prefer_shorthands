name: prefer_shorthands
description: An analyzer plugin that suggests using Dart's dot shorthand syntax. Flutter 3.44+ compatible fork.
version: 0.4.8
homepage: https://github.com/NurmuhammadMahmudov/ndev_prefer_shorthands
repository: https://github.com/NurmuhammadMahmudov/ndev_prefer_shorthands
issue_tracker: https://github.com/NurmuhammadMahmudov/ndev_prefer_shorthands/issues
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  analysis_server_plugin: ^0.3.15
  analyzer: ">=9.0.0 <14.0.0"
  analyzer_plugin: ^0.14.9
  meta: ^1.3.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
